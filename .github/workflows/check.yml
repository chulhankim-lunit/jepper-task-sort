name: Cool CHECK

on:
  push:
    branches:
      - feature/*

jobs:
  deploy:
    runs-on: ubuntu-24.04
    env:
      ABC: ${{ github.ref_name }}
      ABC2: hello_world:${{ github.ref_name }}
    steps:
      - name: Echo
        run: |
          echo "ABC: $ABC"
          echo "ABC2: $ABC2"
